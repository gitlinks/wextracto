language: python
before-install:
- curl -sSf https://gitlinks.github.io/cl-bins/install.sh | sh
install:
- pip install tox
script:
- tox
- "~/.gitlinks/hermes-ci-rust"
env:
- GITLINKS_USERNAME=travis@gitlinks.com
  matrix:
  - TOXENV=py27
  - TOXENV=py34
